// && -> and
// || -> or
// ! -> reverse


let isLoggedIn = true;
let isPaid = false;

console.log(isLoggedIn && isPaid); // If i want to check both the conditions are we use &&  - false because isPaid is false


let isEmailUser = true;
let isGoogleUSer = false;

console.log(isEmailUser || isGoogleUSer); // If you want either one condition satisfy true use || - true
